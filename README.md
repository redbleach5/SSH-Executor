# SSH Executor

[![GitHub](https://img.shields.io/badge/GitHub-Repository-blue?logo=github)](https://github.com/redbleach5/SSH-Executor)

Приложение для управления SSH подключениями и выполнения команд на удаленных хостах.

## Описание

SSH Executor - это десктопное приложение, построенное на базе Tauri (React + Rust), предназначенное для эффективного управления множественными SSH подключениями и выполнения команд на удаленных хостах. Приложение оптимизировано для работы с большим количеством устройств и поддерживает пакетное выполнение команд, экспорт результатов и расширенные функции безопасности.

## Основные возможности

### 🔌 Управление подключениями
- Загрузка списка хостов из файлов (CSV, Excel XLSX, JSON)
- Drag & Drop для загрузки файлов
- Поддержка группировки хостов по тегам и цветовая маркировка
- Автоматическое обновление списка хостов с настраиваемым интервалом
- Тестирование SSH подключений перед выполнением команд
- Поддержка аутентификации по паролю, OpenSSH-ключам и PPK-ключам (PuTTY)
- Автоматическая конвертация PPK ключей в OpenSSH формат
- Поддержка passphrase для защищенных ключей

### ⚡ Выполнение команд
- Одиночное выполнение команд на выбранном хосте
- Пакетное выполнение команд на множестве хостов одновременно
- Параллельное выполнение с настраиваемым количеством одновременных подключений (до 200)
- Отмена выполнения команд в любой момент
- Автоматические повторные попытки для неуспешных хостов
- История выполненных команд с автодополнением
- Валидация команд перед выполнением (с возможностью отключения)
- Отображение прогресса выполнения в реальном времени

### 📊 Экспорт и анализ
- Экспорт результатов в Excel (XLSX), CSV, JSON и HTML
- Настраиваемые столбцы экспорта (хост, ID ТС, статус, код выхода, вывод, ошибки, время, команда)
- Настраиваемый порядок столбцов
- Автоматическая очистка старых результатов
- Фильтрация и поиск по результатам
- Визуализация статуса выполнения

### 🔒 Безопасность
- Шифрование паролей с использованием AES-256-GCM
- Постоянное хранение ключа шифрования между сессиями
- Журнал аудита всех действий с настраиваемым уровнем логирования
- Автоматический выход при неактивности (настраиваемый таймаут)
- Защита настроек паролем
- Безопасное хранение учетных данных
- Очистка буфера обмена после использования
- Zeroize для безопасного удаления паролей из памяти

### 🎨 Интерфейс
- Современный и интуитивный UI
- Поддержка светлой и темной темы
- 3 режима плотности интерфейса (компактный, обычный, просторный)
- Настраиваемый размер шрифта
- Виртуализация для работы с большими списками (тысячи хостов)
- Системный трей с возможностью минимизации
- Уведомления о статусе операций
- Консоль отладки для диагностики

### ⚙️ Настройки
- Гибкая настройка параметров подключения (таймауты, keep-alive, повторные попытки)
- Настройка сжатия SSH соединений
- Настройка производительности и кэширования
- Настройка уведомлений и звуков
- Настройка экспорта данных
- Управление историей команд
- Настройка журнала аудита

## Технологический стек

### Frontend
- **React 18.2** - UI библиотека
- **TypeScript 5.3** - типизация
- **Tailwind CSS 3.3** - стилизация
- **React Router 6** - маршрутизация
- **Zustand 4.4** - управление состоянием
- **TanStack Query 5** - управление данными
- **TanStack Virtual 3** - виртуализация списков
- **React Hot Toast** - уведомления
- **ExcelJS 4.4** - экспорт в Excel
- **PapaParse 5.4** - парсинг CSV
- **React Dropzone** - Drag & Drop загрузка файлов
- **date-fns** - работа с датами
- **Vite 7.3** - сборщик

### Backend
- **Tauri 1.5** - фреймворк для десктопных приложений
- **Rust 2021 Edition** - системный язык программирования
- **ssh2 0.9** - SSH клиент
- **Tokio** - асинхронный runtime
- **Rayon 1.8** - параллельные вычисления
- **AES-GCM 0.10** - шифрование (AES-256)
- **Calamine 0.24** - парсинг Excel файлов
- **Chrono 0.4** - работа с датами
- **bcrypt 0.15** - хеширование паролей
- **csv 1.3** - работа с CSV файлами
- **zeroize 1.7** - безопасное удаление данных из памяти
- **parking_lot 0.12** - быстрые примитивы синхронизации
- **lru 0.12** - LRU кэширование

## Требования

### Для разработки
- **Node.js** 18+ и npm
- **Rust** 1.70+ и Cargo
- **Git** для загрузки зависимостей
- **Windows 10/11** (основная платформа)

### Для использования
- **Windows 10/11**
- Минимум 4 GB RAM
- 100 MB свободного места на диске

## Быстрый старт

### Установка зависимостей

```bash
# Установка Node.js зависимостей
npm install

# Настройка Cargo для работы с SSL (если возникают проблемы)
npm run fix-cargo-ssl
```

### Запуск в режиме разработки

```bash
# Запуск приложения
npm run tauri:dev
```

### Сборка для продакшена

```bash
# Сборка приложения
npm run tauri:build
```

## Структура проекта

```
SSH Executor/
├── src/                    # Frontend код (React + TypeScript)
│   ├── components/         # React компоненты
│   │   ├── icons/          # Иконки приложения
│   │   ├── Layout.tsx      # Основной лейаут
│   │   ├── ConfirmDialog.tsx
│   │   ├── DebugConsole.tsx
│   │   └── ...
│   ├── pages/              # Страницы приложения
│   │   ├── Workspace.tsx   # Основное рабочее пространство
│   │   ├── Settings.tsx    # Настройки
│   │   └── AuditLogs.tsx   # Журнал аудита
│   ├── utils/              # Утилиты
│   │   ├── settings.ts     # Работа с настройками
│   │   ├── excel.ts        # Экспорт в Excel
│   │   ├── validation.ts   # Валидация данных
│   │   └── ...
│   └── types/              # TypeScript типы
├── src-tauri/              # Backend код (Rust)
│   ├── src/                # Rust модули
│   │   ├── main.rs         # Точка входа
│   │   ├── commands.rs     # Tauri команды
│   │   ├── ssh.rs          # SSH функциональность
│   │   ├── security.rs     # Шифрование
│   │   ├── audit.rs        # Журнал аудита
│   │   ├── file_parser.rs  # Парсинг файлов хостов
│   │   └── excel_export.rs # Экспорт результатов
│   ├── Cargo.toml          # Зависимости Rust
│   └── tauri.conf.json     # Конфигурация Tauri
├── scripts/                # Вспомогательные скрипты
└── package.json            # Зависимости Node.js
```

## Использование

### Загрузка хостов

1. Перейдите в Workspace
2. Нажмите "Загрузить файл" или перетащите файл с хостами в область загрузки
3. Поддерживаемые форматы: CSV, Excel (XLSX), JSON
4. Хосты автоматически загрузятся и отобразятся в списке

### Формат файла хостов

**CSV формат:**
```csv
ip,port,hostname,group,tags
192.168.1.1,22,server-01,group1,"tag1,tag2"
192.168.1.2,22,server-02,group2,"tag3"
```

**Excel формат:**
- Первая строка должна содержать заголовки: ip, port, hostname, group, tags
- Последующие строки содержат данные хостов

**JSON формат:**
```json
[
  {
    "ip": "192.168.1.1",
    "port": 22,
    "hostname": "server-01",
    "group": "group1",
    "tags": ["tag1", "tag2"]
  }
]
```

### Выполнение команд

#### Одиночное выполнение
1. Выберите режим "Одиночное" 
2. Выберите хост из списка
3. Введите команду в поле ввода
4. Нажмите "Выполнить" или Enter
5. Результат отобразится в области результатов

#### Пакетное выполнение
1. Переключитесь в режим "Пакетное выполнение"
2. Выберите хосты (можно выбрать несколько или все)
3. Введите команду
4. Настройте параметры параллельного выполнения (количество потоков)
5. Нажмите "Выполнить на выбранных хостах"
6. Следите за прогрессом в реальном времени

### Экспорт результатов

1. После выполнения команд нажмите "Экспорт"
2. Выберите формат (Excel XLSX, CSV, JSON, HTML)
3. Выберите место сохранения
4. Файл будет создан с результатами всех выполненных команд

## Настройки

Приложение предоставляет широкий спектр настроек:

### Подключение
- Порт по умолчанию
- Таймаут подключения (1-300 секунд)
- Количество одновременных подключений (1-200)
- Интервал keep-alive
- Количество повторных попыток
- Настройки сжатия

### Интерфейс
- Тема (светлая/темная)
- Размер шрифта
- Плотность интерфейса (компактная/обычная/просторная)
- Настройки уведомлений

### Безопасность
- Шифрование паролей (включено по умолчанию)
- Журнал аудита
- Таймаут сессии
- Защита настроек паролем
- Очистка буфера обмена

### Экспорт
- Формат по умолчанию
- Выбор столбцов для экспорта
- Порядок столбцов
- Включение заголовков

### Производительность
- Размер буфера виртуализации
- Автоочистка результатов
- Срок хранения результатов
- Кэширование

## Безопасность

Приложение реализует множество функций безопасности:

- **Шифрование паролей**: Все пароли шифруются с использованием AES-256-GCM
- **Постоянный ключ шифрования**: Ключ сохраняется между сессиями для работы с сохраненными данными
- **Журнал аудита**: Все действия записываются с настраиваемым уровнем детализации
- **Автоматический выход**: При неактивности пользователя
- **Защита настроек**: Возможность установить пароль для доступа к настройкам
- **Безопасное хранение**: Данные хранятся в защищенной директории приложения
- **Zeroize**: Пароли удаляются из памяти сразу после использования

## Разработка

### Требования для разработки

1. Установите Node.js 18+ и npm
2. Установите Rust и Cargo
3. Клонируйте репозиторий
4. Установите зависимости: `npm install`

### Структура кода

- **Frontend**: React компоненты в `src/`
- **Backend**: Rust модули в `src-tauri/src/`
- **Коммуникация**: Tauri команды для взаимодействия между frontend и backend

### Скрипты

- `npm run dev` - запуск Vite dev сервера
- `npm run tauri:dev` - запуск приложения в режиме разработки
- `npm run tauri:build` - сборка приложения
- `npm run fix-cargo-ssl` - исправление проблем с SSL в Cargo

## Устранение неполадок

### Проблемы с SSL при сборке

Если возникают проблемы с SSL при сборке Rust зависимостей:

1. Запустите `npm run fix-cargo-ssl`
2. Проверьте настройки в `src-tauri/.cargo/config.toml`
3. Обновите сертификаты Windows

### Проблемы с подключением

- Проверьте правильность IP адресов и портов
- Убедитесь, что SSH сервис запущен на удаленных хостах
- Проверьте учетные данные (пароль или SSH ключ)
- Проверьте настройки файрвола
- Убедитесь, что SSH ключ соответствует формату (OpenSSH или PPK)
- Проверьте правильность passphrase для защищенных ключей

### Проблемы с производительностью

- Уменьшите количество одновременных подключений
- Включите виртуализацию в настройках
- Настройте автоочистку результатов
- Увеличьте таймаут для медленных соединений

## Лицензия

MIT License - см. [LICENSE](./LICENSE)

## Поддержка

Для вопросов и проблем создавайте issues в репозитории проекта.

## Версия

Текущая версия: **1.0.0**
